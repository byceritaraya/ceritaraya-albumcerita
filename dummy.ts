import { uploadCoverImageAction, updateEventAction } from './src/app/admin/(protected)/events/[eventId]/actions.ts';
// Wait, I can't easily import a server action directly into a node script because of "use server" and Next.js dependencies.
